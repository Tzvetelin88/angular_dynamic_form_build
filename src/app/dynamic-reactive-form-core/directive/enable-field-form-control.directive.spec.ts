import { EnableFieldFormControlDirective } from './enable-field-form-control.directive';

describe('EnableFieldFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableFieldFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
